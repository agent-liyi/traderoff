name: Deploy

on:
  push:
    branches: [main]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Deploy to server
        if: success()
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          fingerprint: SHA256:aFIh/O2RJch6USPG7hcnqtFKH/PeLPsNlVGwJKp+WLg
          script: |
            set -e
            cd /app/podcast-site
            git fetch origin
            git reset --hard origin/main
            npm ci
            npm run build
            pm2 reload dongtaipingheng
            echo "✓ Deployed $(git log --oneline -1)"
